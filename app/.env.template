# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.example.mongodb.net/
MONGODB_DB=hostel_demand
MONGODB_COLLECTION=historical_data

# App Settings (Optional)
DEBUG=False
CACHE_TTL=3600  # Cache timeout in seconds
MAX_ROWS_DISPLAY=1000

# Note: Replace the MONGODB_URI with your actual connection string
# DO NOT commit your actual credentials to version control!
# This is just a template - copy this file to .env and add your real credentials